1, 바이오마커 AI 진단 (제거)
의사 대시보드 _ Quick Actions _ AI Analysis (바이오마커 AI 진단) 
의 연결페이지 http://localhost:3000/doctor/biomarker-analysis
를
http://localhost:3000/antigen-test
로 변경하시오 

2, 항원 검사 결과(페이지 구성변경)
http://localhost:3000/antigen-test/62
의 현 화면구성은 유지하되 

예측 결과 분석 블록 안에 항목별로 '체크박스'를 만들고 
체크된 항목을 저장할수있는 '저장'버튼을 만드시오 

'예측 결과 분석'블록 아래에 '이전 예측 결과 분석'을 읽을수 RD(crud) 할수있는 '이전 검사 결과'블록을 만드시오 
# 만약 DB에 예측 결과를 저장하고 불러오는 로직이 없다면 이것도 함께 생성하시오

>>>


**역할:**
당신은 숙련된 풀스택 웹 개발자입니다. 현재 개발 중인 의료용 CDSS(임상 의사 결정 지원 시스템) 웹 애플리케이션의 프론트엔드 UI 수정과 백엔드 데이터 로직 구현을 요청합니다.

**작업 목표:**
의사 대시보드의 네비게이션 링크를 수정하고, 항원 검사 결과 상세 페이지의 UI/UX를 개선하며, 관련 데이터 저장을 위한 백엔드 로직을 구현해야 합니다.

**상세 요구사항:**

**1. 라우팅 및 네비게이션 변경**
* **위치:** 의사 대시보드 (`/doctor/biomarker-analysis`) 페이지 내 'Quick Actions' 섹션.
* **변경 사항:**
    * 기존의 '바이오마커 AI 진단' 메뉴를 제거하거나 링크를 변경하세요.
    * 'AI Analysis' 버튼(또는 링크) 클릭 시 이동 경로를 기존 `.../biomarker-analysis`에서 **`http://localhost:3000/antigen-test`**로 변경하세요.

**2. 항원 검사 결과 페이지 (`/antigen-test/[id]`) 기능 개선**
* **대상 페이지:** `http://localhost:3000/antigen-test/62` 와 같은 상세 결과 페이지.
* **UI 구성 변경:**
    1.  **체크박스 및 저장 기능:**
        * 기존 '예측 결과 분석' 블록 내부의 각 분석 항목 옆에 '체크박스'를 추가하세요.
        * 사용자가 원하는 항목을 체크한 뒤, 이를 DB에 저장할 수 있는 **'저장' 버튼**을 해당 블록 내에 생성하세요.
    2.  **이전 검사 결과 (History) 블록 추가:**
        * '예측 결과 분석' 블록 하단에 **'이전 검사 결과'** 섹션을 새로 만드세요.
        * 여기서는 과거에 저장된 예측 결과 분석 내역을 리스트 형태로 보여주어야 합니다 (Read).
        * 각 기록을 삭제할 수 있는 기능(Delete)을 포함하세요. (CRUD 중 Read/Delete 구현)

**3. 백엔드 및 데이터베이스 로직 구현 (필수)**
* **조건:** 만약 현재 체크된 예측 결과 항목을 저장하고 불러오는 DB 테이블(모델)이나 API가 없다면, 이를 **새로 생성**해 주세요.
* **요구 사항:**
    * `Patient` 또는 `TestResult`와 연동되는 예측 결과 저장용 모델(Schema) 설계.
    * 선택된 항목을 저장(Create), 조회(Read), 삭제(Delete)할 수 있는 API 로직 작성.

**출력 형식:**
* 수정이 필요한 파일명과 해당 코드 블록을 명시해 주세요.
* 새로 추가되는 백엔드 모델이나 API 코드는 별도로 분리하여 작성해 주세요.
* 기존 코드의 맥락을 유지하면서 변경된 부분만 명확히 설명해 주세요.

---
>>>
